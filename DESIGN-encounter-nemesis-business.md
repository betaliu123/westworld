# 设计文档：统一遭遇管线 · Nemesis 势力系统 · 帮派经营 · 驻地

> 状态：**v2 设计稿**。已纳入评审意见（洗脑卧底、剧场融入、警长势力、产业抢夺、帮派驻地、NPC 日程）。
> 「零、前置事实」是查实的现状，会改变优先级判断，请先读。

---

## 零、前置事实（查实，非推测）

### 0.1 StoryTree 基本没在运行

| 事实 | 证据 |
|---|---|
| 共 4 棵树 / 24 节点 | `storyData.js:623` |
| 2 棵永不启动 | ST03 需 `npc_dead(npc_eli)` 但 `NPCRegistry.kill` 零调用；ST11 条件读 `facts.outcome`，写入端是 `facts.result`（`DailySimulation.js:118`） |
| 能启动的 2 棵在第一个选择点永久卡死 | 超时默认选项被包在 `shouldAutoAdvance && node.canAutoAdvance` 内，而这类节点 `canAutoAdvance===false` → 恒假（`StoryRuntime.js:404-411`） |
| **扣钱不扣钱** | `steal_money` 写 worldState 镜像，真钱包是 `economy.money`；`syncToGame` 零调用（`WorldState.js:241`），每晚 Step1 还反向覆盖 |
| 5 个抉择节点全送不到玩家面前 | 首选通道是 `location`/`hq`，而 `checkLocationTriggers` 唯一调用点在室外分支（`main.js:2928`，室内已在 `:2754` return），传入 `currentLocation` 恒 null、`isInHQ` 恒 false |
| `node.preconditions` 从不校验 | `checkPreconditions()` 零外部调用（`StoryRuntime.js:146`） |

**含义**：「艾琳走了，账上钱少了」是 LLM 对一件**机械上没发生的事**的旁白。

### 0.2 帮派系统是展示壳

招募按 R **只弹 toast 不写数据**（`main.js:2370-2385`）→ `members` 恒空 → 成员收益/派遣全锁死；
`influence` 恒 10、`hqLevel` 恒 1 → `OperationSystem` 六种派遣行动不可达；
四支柱**只涨不跌**（玩家最多扣 −13，黑蹄会每天 `+3×intensity`）；
`checkVictory()` 零调用 → 十日战役没有结局；
`pf.money` 每天 +20 但**无提现/花费入口**；
`CombatFactions` 与 worldState 势力是**平行宇宙**（杀黑蹄会成员不掉支柱）。

**可复用素材**：`npcData.js:876-893` 已手写黑蹄会内斗六维关系（silas↔victor↔rosa 的
trust/fear/debt/resentment/respect），无人读取。这是架构图的现成燃料。

### 0.3 打败后无法交互（对应评审意见 1）

`InteractionSystem.js:93` 与 `:113` 两处 `if (npc.brain.state === "DOWN") continue;`
**把倒地 NPC 完全排除在交互目标之外**。所以「击败后洗脑」机制**目前完全不存在**。

### 0.4 NPC 日程与人设无关（对应评审意见 5）

| 事实 | 证据 |
|---|---|
| 生效的是**按职业的通用表** | `AIBrain.js:62` `schedule: scheduleFor(job)` → `JOB_SCHEDULE`（`gameData.js:1029`） |
| `npcData.js` 里 17 份手写人设日程**从未被读** | `NPCManager.js:57-70` 分配身份时只拷 `id/displayName/job/gang/female`，**不拷 `def.schedule`** |
| 只有 4 个粗时段，所有人同一瞬间切换 | `daySegment()`（`AIBrain.js:37`）：5:00→morning、11:00→noon、17:00→evening、21:00→night |
| 场所极度聚集 | 傍晚 **9/14** 种职业去 `saloon`；夜里 **11/14** 回 `home` |

这就是「同一时间都从一个建筑里刷出来」的确切原因。

### 0.5 买房文案错误（评审意见 6）— ✅ 已修

实际买房是走到待售房门前按 E，或带 `shopItem` 的走集市(M)（`main.js:2219-2223`）。
原文案写「去银行可以购买房产」，已改为正确描述。

---

## 一、统一遭遇管线（Encounter Pipeline）

### 1.1 核心决策

剧场与单 NPC 遭遇是**同一套东西的两种配置**，共用一条呈现管线。

| | AI 剧场 | 单 NPC 遭遇 |
|---|---|---|
| 演员数 | 3–5 | 1–2 |
| 舞台 | 固定镇中心 | **动态设在玩家当前位置** |
| 触发 | 每日 9–12 点 | 剧情就绪 + 玩家进入合适场所 |
| 呈现 | 走过来 → ❗ → F → 中央弹窗 → 多段台词 → 选项 → 真实结算 | **完全相同** |

废弃 `location`/`hq` 定点投放（它要求玩家恰好在对的建筑，正是内容浮不上来的根因）。

### 1.2 六阶段

**1 选角** — 两级决策，避免每次都花 LLM：
- 规则预筛（零成本）→ top 3：场所亲和度 + 距离 + 可用性 + 剧情绑定优先
- LLM 定选：给场所/时段/玩家近期行为/3 个候选档案/节点意图，
  要模型答 `{ npcId, plausible, opener, reason }`
- `plausible: false` → 本次不投放，等换场所再试
- LLM 挂 → 取规则第 1 名 + 剧本预写台词

**2 召唤** — 复用 `AIBrain.takeOver/perform/performArrived/release`。
范例 `TheaterRuntime._takeStage()` 已处理赶出室内、取消报案、防串场令牌、到位超时 `snapTo`。
新用 `StageMap.setCenter(x,z)`（`StageMap.js:54`，目前零调用）把舞台移到玩家脚下。

**3 标记** — 头顶 ❗。照抄 🚨 报案图标（`main.js:3245-3271`）——「按 brain 布尔 flag 挂图标」的现成范式。
现有任务标记只认 `getTrackedTask()` 单例且带 `break`，需新增独立标记源。

**4 交互** — F 键 → 中央模态 → **世界暂停**（把 `isOpen` 加进 `anyModalOpen()` 即免费获得）。
一并修三处暂停泄漏：剧场循环独立注册不查暂停（`main.js:3051`）且用墙钟
（`TheaterRuntime.js:30`）、`#note-popup` 漏出 `anyModalOpen()`、各处 `setTimeout` 用墙钟。

**5 抉择** — 复活 `Conversation.openStoryChoice()`（`Conversation.js:120-159`，**完成度 85%、零调用者**）。
已有动态选项渲染、「稍后决定」兜底、耐心条（上限/回补/三段变色/超时延迟关闭），
且 `main.js:2661` 已把 `conversation.update(dt)` 挂在暂停分支——「世界停了弹窗自己走倒计时」现成。

需补：超时不回调（`:128`）、无不限时模式（`:130`）、选项全同色（`:140` vs `styles.css:683-685`）、
无键盘 1/2/3（抄 `TheaterUI.js:126-137`）、无立绘（`portraits.js` 的 `getAvatar` 可用）。

**多段台词**：点击/空格推进，最后一句才出选项。
实现「先说仰慕你（报纸上看到你干的事）→ 再点 → 问能否加入 → 出接受/拒绝」。

**6 结算** — 统一收口 `applyEncounterEffects()`，**直接操作 `economy`/`reputation`/`factionSystem`，
不再经 worldState 金钱镜像**（那是 §0.1 bug 的根源）。

### 1.3 场所亲和度

16 个可进入场所（`Interiors.js:354`）：酒馆/银行/警长办公室/杂货店/旅馆/赌场/教堂/枪械店/
餐馆/裁缝铺/邮局/理发店/铁匠铺/马厩/报社/医馆

| 场所 | 高亲和 | 剔除 |
|---|---|---|
| 赌场 | 赌徒、赌场经理、歌女 | 牧师 |
| 酒馆 | 酒保、歌女、牛仔、赌徒 | — |
| 教堂 | 牧师、医生 | 赌徒、逃犯 |
| 银行 | 银行家、会计 | 逃犯 |
| 警长办公室 | 警长 | 逃犯、通缉中 |
| 报社 | 记者 | — |
| 医馆 | 医生 | — |
| 马厩 | 马夫、铁匠 | — |

「剔除」不是硬禁，节点可标 `forceVenue: true` 覆盖（「牧师冲进赌场找你」本身就是戏剧点）。

---

## 二、Nemesis 势力系统

### 2.1 卧底获取：三条路径（**修正评审意见 1**）

之前只写了「派自己人去当卧底」，漏掉了 Nemesis 真正的核心。补齐为三条：

#### 路径 A · 战斗击败后洗脑（**Nemesis 核心，优先做**）

这是《暗影魔多》的烙印机制，也是让战斗产生长期意义的关键。

**先解决一个体验问题**：玩家不该需要知道「打穿了几格血」。
改为**两阶段伤亡**，靠**视觉动作**区分，不靠数字。

##### 两阶段伤亡系统

| | 阶段 1 · 伏地重伤 | 阶段 2 · 死亡 |
|---|---|---|
| 数据 | `state = DOWN`，`alive = true` | `alive = false` |
| **视觉** | **侧卧、有呼吸起伏、偶尔抽动/挣扎** | **完全静止、俯卧、体色压暗** |
| 玩家可做 | 搜身 / 补刀 / **收服** / 放走 | 只能搜身 |
| 自行演变 | ① 被别的 NPC 救走（抬走/扶走）<br>② 过一会儿自己爬起来逃跑 | 不变 |
| 存在时长 | 短（几十秒到一两分钟） | **持续到第二天消失** |
| 头顶标记 | ⚡（可招降） | 无 |

**关键**：两阶段的动作必须**一眼可辨**。重伤是「还在动的人」，死亡是「不动的东西」。
现状是两者共用同一套躺平表现（`NPC.js:307-313`），必须拆开。

**从阶段 1 到阶段 2 的路径**：
- 玩家在弹窗里选「补刀」
- 或玩家对伏地者继续攻击（补枪/补拳）

**被救走机制**（阶段 1 独有）：
附近有同势力/高好感 NPC 时，有概率过来把人扶走 → 该 NPC 脱离倒地、离场就医。
这给玩家「要不要赶紧收服」的时间压力。

##### 洗脑交互

```
打倒 NPC（阶段 1）
  ↓ 头顶 ⚡，按 E
中央弹窗「他躺在地上喘气，等着你的处置」
  ├ 洗脑收服 → 成为你的人（原属敌方 → 当场变卧底）
  ├ 搜身     → 现有 loot 行为
  ├ 补刀     → 进入阶段 2（死亡），+通缉，掉敌方 manpower
  └ 放走     → 该 NPC +debt（欠你一条命），后续可能主动投靠
```

**窗口时长**：不设固定秒数，**只要还在阶段 1 就能招降**。
重伤到爬不起来的（`hp` 打穿 2 格以上，现有 `tryReviveFromDown` 机制）等于无限窗口；
轻伤的会自己爬起来跑掉，天然形成时间压力。这样玩家不需要理解数字。

**前置**：必须解开 `InteractionSystem.js:93,113` 对 DOWN 的排除（§0.3）。

**成功率**：
```
基础 40%
+ 打伤程度      (hp 欠得越多越容易，每格 +8%，上限 +32%)
+ 玩家凶名      (wanted 越高越唬人，上限 +15%)
+ 已有好感/人情 (affection/debt，上限 +20%)
− 对方胆量      (bravery × 30%)
− 对方职级      (rank × 8%，老大几乎不可能)
− 对方忠诚      (loyalty × 25%)
夹在 5% ~ 90%
```

**失败后果**：该 NPC `resentment +30`，且**记住这件事**（进 `encounterMemory`），
下次遭遇时口吻敌对；连续失败会让他所属势力对玩家 heat 上升。

**关键设计**：若被洗脑者原属黑蹄会，**表面归属不变** →
`allegiance = { real: "player", apparent: "black_hoof" }` → **他当场变成你的卧底**。
这就是 Nemesis 的精髓：你打服的人回到敌营继续升职，成为你的眼睛。

#### 路径 B · 高好感转化（含**手机远程收服**）

`affection ≥ 60` 且 `trust ≥ 40` → 两个入口：
1. 遭遇管线里出现「邀他为你做事」选项
2. **手机联系人界面直接收服**（新增）—— 不必跑到人面前，够熟就能电话谈

比 A 温和、不掉声望，但慢且要求关系经营。

#### 路径 C · 主动派遣（保留）

自己人 → 架构图上点「派往黑蹄会」。前置 `trust ≥ 50` 且 `debt > 0`。
适合把忠诚的人送进去，但对方 `suspicion` 累积更快（外来者更可疑）。


### 2.2 数据模型

**NPC 新增字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| `rank` | 1–5 | 5=老大 4=副手 3=主管 2=打手 1=小弟（复用 `factionRank`） |
| `reportsTo` | npcId\|null | 直属上级，用于画架构图（**新增**） |
| `ambition` | 0–1 | 野心，决定是否觊觎上级（可由 storyTags `ambitious` 推导） |
| `loyalty` | 0–100 | 对所属势力的忠诚（区别于对玩家的 trust） |
| `allegiance` | `{real, apparent}` | 不一致 = 卧底 |
| `suspicion` | 0–100 | 所属势力对他的怀疑，满 → 清算 |
| `encounterMemory` | 数组 | 与玩家的遭遇记录（复用 `npc.memories`） |

### 2.3 晋升顶替循环

**触发**：职位空出（死亡/放逐/叛变/被清算）

```
候选 = 同势力 rank = 空缺−1 且 loyalty > 30
评分 = respect(对空缺者)×0.3 + ambition×0.4 + loyalty×0.2 − fear(对老大)×0.3 ± 10
```

**关键**：晋升改变此人对玩家的态度。
你杀了他上级 → 他既得利又恐惧（+debt 隐性、+fear）；你和他有交情 → 他上位后成为敌营内的实质盟友。

**播报**：走**报纸**不走手机（避免加剧洪水）：「黑蹄会人事变动：维克托·黑尔接掌赌场事务」

### 2.4 卧底双向

**我方卧底在敌营**：
- 每天提供情报（削弱敌方支柱 / 揭示敌方成员真实 allegiance）
- `suspicion` 每天累积（受其 loyalty 与敌方 `legitimacy` 支柱影响）
- 满值 → 清算。后果按 `suspicion` 溢出程度和敌方 `legitimacy` 决定：**死亡或叛变两种都可能**
- 卧底 `rank` 随时间/立功上升 → 情报质量提高

**敌方卧底在我方**：
- 招募来的人有概率 `allegiance.real = "black_hoof"`
- 表现：正常上班拿薪水，但**每天分流一部分经营收益**给敌方，且敌方支柱回血更快
- 玩家可花钱/花时间**调查**某成员 → 有概率揭示；查错人掉该成员 loyalty
- 线索是**间接**的：「近期与黑蹄会成员有过接触」「账目有小额缺口」「经营收益低于预期」

### 2.5 支柱必须能降（阻塞项）

| 玩家行为 | 支柱 |
|---|---|
| 击杀/放逐/洗脑敌方成员 | `manpower` − |
| 卧底情报 → 揭发腐败 | `legitimacy` − |
| 抢劫赌场 / 打断收入 | `wealth` − |
| 夺取商铺（§四） | `territory` − |
| 离间高层（拉高内部 resentment） | `legitimacy` − 且可能触发内斗晋升 |

同时把 `CombatFactions` 与 worldState 势力打通（杀黑蹄会成员**要真的掉 `manpower`**），
并接通 `checkVictory()` 让十日战役有结局。

### 2.6 组织架构图 UI

**入口**：帮派面板（G）新增 Tab。三栏或 Tab 切换：我的帮派 / 黑蹄会 / 警长势力

```
┌─ 我的帮派 ────────┐ ┌─ 黑蹄会 ─────────┐ ┌─ 警长势力 ───────┐
│      [你] R5      │ │ [塞拉斯·克劳] R5  │ │ [赫克托·布恩] R5 │
│    ┌───┴───┐      │ │      ↓            │ │   ⚠ 被腐蚀 0.6   │
│ [杰克]  [艾琳]    │ │ [维克托·黑尔] R4  │ │      ↓           │
│  R3      R3       │ │  🔥野心 0.8       │ │  [副警长×2]      │
│ 💚忠诚  ❓可疑     │ │      ↓            │ │                  │
│         ↓         │ │ [罗莎·奎因] R3    │ │                  │
│      [小弟×2]     │ │  🕵我方卧底       │ │                  │
└───────────────────┘ └───────────────────┘ └──────────────────┘
```

**节点卡**：头像（`portraits.js` 的 `getAvatar`）+ 名字 + rank + 状态图标
（💚忠诚>70 / 😐一般 / ⚠️不稳<40 / ❓可疑 / 🕵我方卧底 / 🔥野心>0.7）+ `reportsTo` 连线

**点击 → 详情**：六维关系条 + 遭遇记忆（"第 3 天你放过了他"）+ 操作

| 对象 | 操作 |
|---|---|
| 我方成员 | 提拔 / 降职 / 调查 / 派往敌营 / 逐出 |
| 敌方成员 | 离间 / 收买 / 标记暗杀目标 |
| 我方卧底 | 索取情报 / 召回（有风险）/ 命令削弱特定支柱 |

**信息隐藏**：敌方 `loyalty` / `allegiance.real` 默认显示 `???`，需情报解锁。这是投资卧底的动机。

> **评审待确认**：操作深度是否先只做「查看 + 派卧底 + 调查」三个核心？

---

## 三、警长势力（第三方，评审意见 3）

### 3.1 为什么这个第三方成本低

`npc_hector`（赫克托·布恩）已有 `role: "sheriff"`、`storyTags: ["law","compromised","tired"]`。
**`compromised` 本身就是「已被黑蹄会部分收买」** —— 三方张力的素材是现成的。

### 3.2 设计

```
{
  id: "law", name: "警长势力", leaderId: "npc_hector",
  pillars: {
    authority:  { label:"威权", value:60 },   // 镇民是否服管
    manpower:   { label:"人手", value:40 },   // 副警长数量
    evidence:   { label:"证据", value:30 },   // 手上有多少你的把柄
    integrity:  { label:"廉洁", value:35 },   // 被黑蹄会腐蚀程度（低=腐败）
  }
}
```

**与玩家的关系**：直接挂现有 `Reputation.wanted`（通缉度）——已存在，不用新造。
`evidence` 支柱决定通缉的「后果强度」：证据高 → 警长敢抓你；证据低 → 只能口头警告。

**与黑蹄会的关系**：`integrity` 低意味着赫克托被收买。这创造三角：
- 玩家可**揭发腐败** → 同时打击 `law.integrity`（他垮台）和 `black_hoof.legitimacy`（丑闻）
- 玩家可**帮警长清理** → 提升 `law.integrity`，换取通缉减免，但黑蹄会视你为敌
- 玩家可**加深腐败**（贿赂）→ 通缉好摆平，但警长势力越弱，黑蹄会越无人制衡

**三方平衡的乐趣**：你需要警长弱到管不了你，但强到能牵制黑蹄会。

### 3.3 内容需求

需 DS 生成：警长势力的 2-3 名副警长档案（名字/性格/关系）+
`law` 相关的遭遇节点（警长找你谈话/递交证据/索贿）。

---

## 四、产业与经营（评审意见 4）

### 4.1 两种获取方式

**购买/投资**（和平路线）
- 街上商铺可投资：付钱 → 拿股份 → 每日分成
- 不需要战斗，但贵、回本慢
- 投资额分档（小股/大股/独资），份额决定收益与话事权

**抢夺**（暴力路线，现阶段简化）
- 点「抢夺」按钮 → 弹窗 → **按双方势力数值结算** → 成功/失败 + 数值后果
- 不做实时战斗关卡（后续可升级）

**结算公式（简化版）**
```
我方战力 = Σ(派驻打手的 competence) + 玩家战力(等级/装备/弹药)
           + 若有卧底情报 → +20%
守方战力 = 业主势力 manpower支柱×0.5 + territory支柱×0.3 + heat×0.2

成功率 = clamp(我方/(我方+守方), 0.1, 0.9)

成功 → 获得产业；业主势力 territory −8；+通缉；业主势力对你敌对
失败 → 损失 $；有概率伤/失成员；+通缉；业主势力 heat +
```

**弹窗内容**：结算前给出双方战力预估（不给精确数字，给「优势/势均力敌/劣势」）
+ 一个「投入多少人手」的选择 → 让玩家有决策而不是纯掷骰。

### 4.2 数据模型

```
产业 Business {
  id, name, type,        // saloon_share / casino_share / freight / protection
  level: 1..3,
  ownerFaction,          // player / black_hoof / neutral
  posts: [Post],
  baseIncome, upkeep, heat
}

岗位 Post {
  id, label,             // 账房 / 护卫 / 跑腿 / 打手
  preferredRoles: [...], // bookkeeper→账房 加成
  baseYield,
  assignedNpcId
}
```

### 4.3 收益公式

```
岗位产出 = baseYield
         × 角色匹配 (1.0 匹配 / 0.6 不匹配)
         × 能力     (由 personality 推导：账房看智力/反向 greed，护卫看 bravery)
         × 忠诚     (loyalty/100，下限 0.3)
         × 产业等级

若该 NPC 是敌方卧底：
  实际入账 = 产出 × 0.5，另 50% 流向敌方 wealth 支柱
  账面看不出差异 —— 只能通过「总收益低于预期」察觉

帮派净收入 = Σ岗位产出 − Σ维护费 − 薪水(members×10)
```

**必须同时修**：`pf.money` 目前花不出去。开通提现到 `economy` + 支出通道
（升级产业 / 付薪 / 买情报 / 雇佣兵）。

### 4.4 界面（帮派面板内独立 Tab）

```
┌─ 产业：老鹰酒馆（Lv.2） ──────────────┐
│ 预期日收益 $180  维护 $40  净 $140    │
│ ┌────────┬────────┬────────┐          │
│ │ 账房   │ 护卫   │ 跑腿   │          │
│ │[艾琳]  │[杰克]  │ [空]   │          │
│ │ ✓匹配  │ ✓匹配  │        │          │
│ │ $80    │ $60    │  +$40  │          │
│ └────────┴────────┴────────┘          │
│                 [升级 Lv.3 — $1500]   │
└───────────────────────────────────────┘

┌─ 可争取的商铺 ────────────────────────┐
│ 赌场（黑蹄会）  [投资 $800] [抢夺]    │
│ 铁匠铺（中立）  [投资 $300] [抢夺]    │
└───────────────────────────────────────┘

┌─ 待派人员 ────────────────────────────┐
│ [罗莎] 赌徒 · 忠诚 65 · 未派岗        │
│ [老魏] 矿工 · 忠诚 80 · 未派岗        │
└───────────────────────────────────────┘
```

「预期 $180」vs 实际入账 $140 的差额 = 发现卧底的线索（刻意不做成明确报错）。

---

## 五、帮派驻地（评审意见 4b）

### 5.1 设计

参照《荒野大镖客 2》帮派营地：大街一侧、**露天、一圈房子围起来**的院落。

**功能**：
1. **看得见的招募回报** —— 招来的 NPC 在这里干活（擦枪/搬箱/喝酒/守门），
   派了岗的人在对应位置做对应动作
2. **默认复活点** —— 没买房时，睡觉后在驻地醒来（替代现在的警局/街边）
3. **剧情舞台** —— 遭遇管线可把驻地当场所，NPC 来驻地找你谈事
4. **升级可视** —— `hqLevel` 提升 → 院内建筑/设施增加（这让 `hqLevel` 第一次有意义）

### 5.2 实现要点

- 位置：主街一侧空地，需在 `Town` 里划一块并生成围合建筑
- **小地图特殊标记**：一个小房子 icon（区别于现有的任务/建筑标记），
  接入 `getQuestMarkers()` → `Minimap.update`（`Minimap.js:146-166` 已支持 NPC/建筑门/物品三类，加一类）
- NPC 站位：类似 `StageMap` 的站位环，按岗位定义固定点位
- 睡觉逻辑：复活点优先级改为 `自有房产 > 帮派驻地 > 现有兜底`
- 驻地注册成一个「场所」，让遭遇管线的场所亲和度表能用

### 5.3 与经营的关系

驻地本身是 0 级产业（只有维护费没有收益），但它是**岗位的容器**：
早期没有商铺时，成员在驻地做基础活（守卫/杂役）产出微薄收益，
拿下商铺后才能派到更赚钱的岗位。

---

## 六、剧场融入核心循环（评审意见 2）

### 6.1 问题

现在剧场结算只给即时 `cash/honor/wanted`（`TheaterDirector.js:230-241`），
演完就结束，不影响长线。你举的例子正是缺的东西：
**帮萝丝去旧金山 → 应该影响什么？后来才揭示她是警察局长的妹妹 → 警局影响力变化。**

### 6.2 设计：后果包（Consequence Package）

给每棵剧场树的每个结局挂一个后果包：

```
consequences: {
  immediate: { cash, honor, wanted },              // 现有

  faction: {                                        // 新增：势力数值
    law: { authority: +5, integrity: -3 },
    black_hoof: { legitimacy: -4 },
  },

  reveal: {                                         // 新增：延迟揭示（你要的效果）
    afterDays: 2,
    text: "你才知道，萝丝是警察局长的妹妹。",
    channel: "newspaper",                           // 或 encounter（NPC 来告诉你）
    effects: { faction: { law: { authority: +8 } },
               relationship: { npc_hector: { debt: +30 } } }
  },

  delayed: [                                        // 新增：延迟回报
    { afterDays: 5, type: "letter", from: "萝丝",
      text: "旧金山一切都好。这是当初欠你的。",
      effects: { cash: 150 } }
  ],

  unlock: ["npc_rosa_sister_of_sheriff"]            // 新增：解锁隐藏关系标记
}
```

### 6.3 三种后果类型

| 类型 | 作用 | 例子 |
|---|---|---|
| **即时** | 当场钱/声望/通缉 | 现有 |
| **势力** | 改支柱/影响力，喂给 Nemesis 层 | 帮萝丝走 → 赌场少个台柱 → 黑蹄会 `wealth −` |
| **延迟揭示** | N 天后才告诉你真相，回头改变意义 | 萝丝是局长妹妹 → 警长势力 `authority +` |
| **延迟回报** | N 天后来信/来人还人情 | 萝丝寄钱回来 |

**延迟揭示是关键**：它让「当时看起来只是个小事」在几天后产生结构性影响，
这是最便宜的涌现来源——不需要新系统，只需要内容里埋伏笔。

### 6.4 内容需求

8 棵剧场树 × 约 3 个结局 ≈ **24 个后果包**，需 DS 生成。
生成时要求：至少 1/3 的结局带 `reveal`（延迟揭示），让「回头一想原来如此」成为常态体验。

---

## 七、NPC 日程人设化（评审意见 5）

### 7.1 三个问题（§0.4 已查实）

1. `npcData.js` 的 17 份手写日程**从未被读**（`NPCManager` 不拷 `def.schedule`）
2. 只有 4 个粗时段，**所有人同一瞬间切换**
3. 场所极度聚集：傍晚 9/14 职业去酒馆、夜里 11/14 回家

### 7.2 修法

**a. 打通人设日程**
`NPCManager` 分配重要 NPC 身份时补一句拷 `def.schedule` → `personality.schedule`。
一行改动，立刻让 17 个有人设的 NPC 走自己的日程。

**b. 细化时段**
4 段 → 7 段：`dawn(5-7) / morning(7-11) / noon(11-14) / afternoon(14-17) /
evening(17-20) / night(20-23) / latenight(23-5)`

**c. 加个体抖动**
每个 NPC 一个固定偏移 `±0~40 分钟`（由 id 哈希决定，保证同一人每天一致），
避免整镇同一秒切换。

**d. DS 重写人设日程**
为 17 个有人设的 NPC 各写 7 段日程，要求：
- 符合职业与性格（`storyTags`）
- **错开热点**：不能都傍晚去酒馆
- 有「值得尾随」的钩子：比如逃犯深夜去墓地、记者半夜蹲报社、
  被腐蚀的警长每周有一晚去赌场后门
- 输出到独立覆盖层 `npcSchedules.js`（与 `theaterBeatTo.js` 同套路），不改 `npcData.js` 内联

**e. 普通路人**
保留 `JOB_SCHEDULE`，但同样加细时段 + 抖动，并把过度聚集的项打散
（傍晚 9/14 → 分流到餐馆/理发店/邮局等冷门场所）。

---

## 八、手机联系人界面扩展

现有手机联系人只能看聊天记录 + 点固定快捷回复（`Phone.js:311-335`）。补两项：

### 8.1 自由输入框（接 DS）

联系人会话里加输入框，接 `NpcChatService`（已存在，含 `ChatBudget` 限流）。
与街上自由对话共用同一套 LLM 通路，区别只在上下文里注明「这是短信不是面谈」
→ 模型语气应更简短、更像文字消息。

**收益**：让「有联系人」这件事有持续价值，而不只是收通知。

### 8.2 远程收服（路径 B 的入口之二）

联系人详情里，满足 `affection ≥ 60 && trust ≥ 40` 时出现「邀他入伙」按钮。
点击 → 走遭遇管线的抉择弹窗（复用同一个组件，只是不需要 NPC 走过来）。

**设计意图**：把「经营关系」和「扩张势力」直接连起来——
你在手机里养熟的人，可以不见面就招进来。

---

## 九、实施顺序

排序原则：**先让现有东西真的跑起来**，再加新东西；同一批里优先「改动小、可见度高」。

### P0 · 地基
1. 打通金钱效果（让效果直接写 `economy`，放弃 worldState 金钱镜像）
2. 修抉择节点永久卡死（`StoryRuntime.js:404-411`）
3. 招募真的写数据（`main.js:2370-2385`）
4. 支柱能降 + `CombatFactions` 与势力打通（杀敌掉 `manpower`）
5. 修 ST03/ST11 启动条件
6. ✅ 买房文案（已完成）

**验收**：玩到第 8 天钱包真的少过一次；击杀黑蹄会成员后支柱数字下降。

### P0.5 · NPC 日程（插在前面：改动小、可见度极高）
7. `NPCManager` 拷 `def.schedule`（一行）
8. 细时段 + 个体抖动
9. DS 生成 17 份人设日程 → `npcSchedules.js`
10. 打散普通路人的场所聚集

**验收**：傍晚酒馆不再挤满一半镇民；跟着逃犯能看到他深夜去墓地。

### P1 · 统一遭遇管线
11. `EncounterRuntime` 六阶段状态机
12. 复活 `openStoryChoice` + 补 5 缺口 + 多段台词
13. 接入 `anyModalOpen()` 暂停 + 修三处泄漏
14. ❗ 头顶标记
15. 场所亲和度 + LLM 选角与合理性判定（带规则降级）
16. 剧场呈现层迁移，淘汰猴补丁

**验收**：进赌场，合理身份的 NPC 走过来、头顶 ❗、按 F 弹中央框、多段台词后出选项、
选完钱/声望真变。牧师不会在赌场谈信仰。

### P2 · 战斗洗脑卧底（Nemesis 入口）
17. **两阶段伤亡**：拆分「伏地重伤」与「死亡」的视觉表现（呼吸起伏/抽动 vs 完全静止压暗）
18. 伏地阶段的自行演变：被同伙救走 / 自己爬起来逃跑；死亡阶段持续到次日消失
19. 解开 `InteractionSystem` 对 DOWN 的排除
20. ⚡ 标记 + 四选项弹窗（走遭遇管线）；窗口 = 整个伏地阶段，不设固定秒数
21. 洗脑成功率公式 + 失败记仇（`encounterMemory`）
22. `allegiance` 双字段 + 「打服敌方成员 → 当场变卧底」

**验收**：打倒维克托后能一眼看出他还活着（在动），按 E 招降；
补刀后变成一动不动的尸体、躺到第二天；成功收服后他回黑蹄会上班，架构图标 🕵。

### P2.5 · 手机扩展
23. 联系人自由输入框（接 `NpcChatService`）
24. 远程收服按钮（好感门槛）

### P3 · 消息治理
25. 优先级/分类字段 + 分时投递 + 限流 + 未读徽章 + few-shot 去名字

### P4 · Nemesis 完整循环
22. `reportsTo`/`ambition`/`loyalty`/`suspicion` 字段
23. 晋升顶替 + 报纸播报
24. 敌方卧底 + 调查机制
25. 组织架构图 UI
26. 接通 `checkVictory()`

### P5 · 警长势力
27. `law` 势力数据 + 四支柱
28. 挂现有 `wanted`；`evidence` 决定通缉后果强度
29. DS 生成副警长档案 + 警长遭遇节点
30. 三方平衡（揭发腐败/帮清理/加深腐败）

### P6 · 帮派驻地
31. 主街一侧生成围合院落 + 注册为场所
32. 成员站位与干活动作
33. 复活点优先级：自有房产 > 驻地 > 兜底
34. `hqLevel` 升级改变院内设施

### P7 · 产业与经营
35. 产业/岗位模型 + 收益公式 + 卧底分流
36. 投资（购买）路线
37. 抢夺结算弹窗
38. 经营界面
39. `pf.money` 提现与支出

### P8 · 剧场融入
40. 后果包数据结构（immediate/faction/reveal/delayed/unlock）
41. 延迟队列（N 天后触发揭示与回报）
42. DS 生成 24 个后果包

### P9 · 内容扩容
43. 加故事树

---

## 十、已确认的决策

| # | 决策 |
|---|---|
| 1 | 金钱效果**直接写 `economy`/`reputation`**，弃用 worldState 金钱镜像 |
| 2 | **两阶段伤亡**：伏地重伤（可搜身/补刀/收服/放走，会被救走或自己爬起来）→ 死亡（尸体持续到次日）。两阶段视觉必须一眼可辨 |
| 3 | 洗脑窗口 = **整个伏地阶段**，不设固定秒数（玩家无需理解血量数字） |
| 4 | 手机联系人界面：加**自由输入框**（接 DS）+ **远程收服**（好感门槛） |
| 5 | **加警长势力**作为第三方 |
| 6 | 产业**既可购买/投资，也可抢夺**；抢夺现阶段简化为弹窗数值结算 |
| 7 | 加**帮派驻地**（露天围合院落，在主街一侧），**小地图用小房子 icon 标记**；无房产时睡觉在驻地醒来 |
| 8 | NPC 日程用 DS 按人设重写，提到 P0.5 优先做 |
| 9 | 买房文案改为集市/房门前购买（✅ 已完成） |

## 十一、待确认（剩余）

1. **钱被偷的呈现**：给可介入抉择（提前一天征兆 → 发现她在收拾行李 → 拦住/装看不见/开枪，
   需把背叛节点拆两个）；还是只要事后强提示？
2. **架构图操作深度**：先只做「查看 + 派卧底 + 调查」，还是一次做全 §2.6 的 12 个操作？
3. **驻地具体位置**：我会先看地图布局自行选点，若不满意再调。
